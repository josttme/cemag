---
// src/components/astro/ProductCard.astro
import type { Product } from '@/lib/data';
import { Image } from 'astro:assets';

interface Props {
  product: Product;
}

const { product } = Astro.props;

// Usamos nuestra función utilitaria si la creaste, o Intl directo
const formattedPrice = new Intl.NumberFormat('es-AR', {
  style: 'currency',
  currency: 'ARS',
  maximumFractionDigits: 0,
}).format(product.price);

// Extraemos la URL de la primera imagen de la primera variante (Best Practice de fallback)
const finalImageUrl = product.variants[0]?.images[0]?.url || '/placeholder.jpg';
---

<a
  href={`/productos/${product.id}`}
  data-astro-prefetch="hover"
  class="group flex flex-col cursor-pointer"
>
  <div class="relative w-full aspect-square bg-[#f6f6f6] overflow-hidden mb-3">
    <Image
      src={finalImageUrl}
      alt={`Fotografía de ${product.name}`}
      inferSize={true}
      class="h-full w-full object-cover object-center transition-transform duration-300 group-hover:scale-105"
    />
  </div>
  <div class="flex flex-col gap-1 px-1">
    <h3 class="text-slate-900 font-medium text-base leading-tight">
      {product.name}
    </h3>
    <p class="text-slate-900 font-medium text-base mt-1">
      {formattedPrice}
    </p>
  </div>
</a>
